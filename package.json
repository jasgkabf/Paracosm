{
  "name": "paracosm",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @paracosm/shared build && turbo run dev",
    "dev:api": "pnpm --filter @paracosm/api dev",
    "dev:web": "pnpm --filter @paracosm/web dev",
    "dev:cli": "pnpm --filter @paracosm/cli dev",
    "build": "pnpm --filter @paracosm/shared build && turbo run build --filter=!@paracosm/shared",
    "start": "pnpm --filter @paracosm/api start & sleep 2 && pnpm --filter @paracosm/web start",
    "start:api": "pnpm --filter @paracosm/api start",
    "start:web": "pnpm --filter @paracosm/web start",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "turbo": "^2.1.0",
    "eslint": "^8.57.0",
    "prettier": "^3.3.0",
    "@types/node": "^20.14.0"
  },
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
